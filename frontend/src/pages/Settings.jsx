import React,{useState} from "react";
import {Save,UserRound,ShieldCheck,LogOut} from "lucide-react";
import {useAuth} from "../context/AuthContext";
export default function Settings(){const {user,updateProfile,logout}=useAuth();const [name,setName]=useState(user.name||"");const [phone,setPhone]=useState(user.phone||"");const [saved,setSaved]=useState(false);const submit=e=>{e.preventDefault();updateProfile({name,phone});setSaved(true);setTimeout(()=>setSaved(false),1800)};
return <div className="page-stack"><section className="section-heading"><div><span className="eyebrow">ACCOUNT</span><h2>Settings</h2><p>Update your profile and review your current access role.</p></div></section><div className="grid gap-6 lg:grid-cols-[1fr_340px]"><form onSubmit={submit} className="panel space-y-5"><div className="panel-header"><div><span className="eyebrow">PROFILE</span><h3>Personal information</h3></div><UserRound/></div><label className="field"><span>Full name</span><input value={name} onChange={e=>setName(e.target.value)}/></label><label className="field"><span>Phone</span><input value={phone} onChange={e=>setPhone(e.target.value)}/></label><label className="field"><span>Email</span><input value={user.email} disabled/></label><button className="btn btn-primary"><Save size={16}/>{saved?"Saved":"Save changes"}</button></form><div className="panel"><div className="panel-header"><div><span className="eyebrow">ACCESS</span><h3>Current role</h3></div><ShieldCheck/></div><div className="role-card"><strong>{user.role.toUpperCase()}</strong><span>{user.role==="admin"?"Full system administration":user.role==="authority"?"Case review and analytics":"Assessment submission and personal records"}</span></div><button onClick={logout} className="btn btn-outline mt-5 w-full"><LogOut size={16}/> Sign out</button></div></div></div>}
